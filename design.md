# mindlens 디자인 시스템 — Furiosa 차용 (정본)

> **정본 규칙:** 이 문서가 디자인 시스템 SoT다. 토큰 값을 고칠 땐 여기서 고치고 `web/styles/globals.css`의 `@theme`·`tailwind.config.ts`를 **같이** 갱신한다(둘은 이 문서의 구현). 구현 중 "느낌"으로 색·간격을 바꾸지 말 것 — 먼저 이 문서를 고친다. (하네스 §2·§4)
>
> **뿌리:** [furiosa.ai](https://furiosa.ai) 라이브 CSS 변수에서 추출한 실제 브랜드 토큰을 채택했다. 하드웨어(RNGD)와 제품(mindlens)이 한 몸으로 보이게 하려는 의도적 선택.
>
> **이전 이력:** 이 파일은 원래 무관한 "Boram(#2c5cff)" 스펙이었다(리디자인 전 stale). 2026-07-23 전면 교체. 리디자인 결정 배경 = `docs/specs/2026-07-23-mindlens-redesign-design.md`.

---

## 0. 큰 원칙 — 이원화 (도구 vs 대화)

mindlens는 청중이 둘이라 **한 시스템, 두 표면**으로 간다. 색이 화면의 성격을 가른다.

| 표면 | 대상 | 스킨 | 그라운드 | 라디우스 | 인상 |
|---|---|---|---|---|---|
| **의뢰자** (대시보드·가이드·결과·**벤치마크**) | 리서처(도구 사용) | **Furiosa 정통** | 화이트/페이퍼 | 8–10px (타이트) | 데이터·신뢰·하드웨어 시그널 |
| **응답자** (동의·스크리너·인터뷰·완료) | 인터뷰 참가자(대화) | **Furiosa 웜** | 크림/블러시 | 12–16px (부드럽게) | 사람·따뜻함·안심 |

- **레드는 두 표면 공통 액센트**지만, 도구 쪽은 절제된 액션·데이터 강조로, 대화 쪽은 따뜻한 액션으로 쓴다.
- **블랙 드라마(풀블리드 검정 히어로) 배제** — obsidian은 텍스트·아이콘·소형 요소에만. 그라운드는 항상 라이트.
- 랜딩/마케팅은 의뢰자 톤(라이트·레드)을 따르되 카피는 따뜻하게.

---

## 1. 색 토큰

### 브랜드 코어 (furiosa.ai 실측)
| 토큰 | 값 | 용도 |
|---|---|---|
| `brand-red` | `#E21500` | **단일 브랜드 액센트** — 액션·링크·활성·데이터 강조·브랜드 마크 |
| `red-dark` | `#BC1302` | hover/pressed |
| `obsidian` | `#151515` | 본문·제목 텍스트, 소형 진한 요소 |
| `charcoal` | `#444444` | 보조 본문 |
| `grey` | `#7F7F7F` | 메타·비활성 라벨 |
| `silver` | `#D4D4D4` | 1px 보더·구분선 |
| `platinum` | `#E1E1E1` | 옅은 보더·구분 |
| `black` | `#0A0A0A` | 텍스트/아이콘 한정(그라운드로 쓰지 않음) |

### 그라운드
| 토큰 | 값 | 용도 |
|---|---|---|
| `canvas` | `#FFFFFF` | 기본 표면 |
| `paper` | `#FAF9F7` | 살짝 웜 바이어스 중립 밴드(섹션 교대) |

### 응답자 웜
| 토큰 | 값 | 용도 |
|---|---|---|
| `cream` | `#FFFBF6` | 응답자 캔버스 |
| `blush` | `#FFF3EE` | 웜 wash(배지·칩·자극물 배경) |
| `warm-border` | `#F0E6DC` | 웜 카드 보더 |
| `warm-ink-soft` | `#8A6F5F` | 웜 보조 텍스트 |

### 데이터 · 바이브런트 (차트·센티먼트·카테고리 — furiosa.ai 실측)
`mint #70E697` · `cyan #76D6FF` · `peach #FEC2A0` · `lilac #CDBBFF` · `lemon #FFFA82` · `orange #FF9A52`
- 센티먼트 매핑(예): 긍정=mint · 중립=grey · 우려=orange · 부정=red.
- 벤치마크: **NPU=brand-red, GPU=grey/silver**(대조).

### 시맨틱 (액센트와 분리 — 액센트로 세지 않음)
| 토큰 | 값 | 비고 |
|---|---|---|
| `success` | `#00AA64` (또는 mint 계열) | 긍정·완료 |
| `warning` | `#FF9A52` (orange) | 주의 |
| `danger`(error) | **`#6F2020` maroon** | **brand-red와 충돌 방지 — 에러는 maroon 계열로 분리.** 기존 `#ef4444` 폐기 |

---

## 2. 타이포그래피

- **서체: Pretendard**(한글+라틴 단일 패밀리). ABC Favorit(Furiosa 독점·한글 없음) 대체. 웹폰트는 `globals.css`에서 로컬/셀프호스팅.
- **mono: 시스템 monospace**(`ui-monospace, "SF Mono", Menlo, Consolas, monospace`) — **오직 벤치마크 텔레메트리/지표에만.** 그 외 mono 역할(메타·라벨)은 Pretendard + `tabular-nums`.
- **시그니처: 크고 얇은(400) 타이트한 제목** + 본문 500.

| 역할 | 크기 | 굵기 | 자간 | 비고 |
|---|---|---|---|---|
| display | clamp(32→54px) | 400 | -0.03em | 히어로. 얇고 타이트 |
| title | 24–28px | 600 | -0.02em | 화면 제목 |
| section | 18–19px | 600 | -0.01em | 섹션 제목 |
| body | 15px | 500 | 0 | 본문 |
| meta | 12–13px | 500 | 0 | Pretendard + `tabular-nums` |
| eyebrow | 11–12px | 500–600 | +0.1em, UPPER | mono 렌더(라벨) |
| telemetry | 12–13px | 400 | 0 | **시스템 mono**(벤치마크 전용) |

숫자 정렬 필요한 곳 어디나 `font-variant-numeric: tabular-nums`.

---

## 3. 형태 · 깊이

- **라디우스:** 버튼/입력 8px · 의뢰자 카드 10px · 응답자(웜) 카드 12–16px · 배지 6px · pill 9999px(탭/칩).
- **보더:** 1px `silver`/`platinum` 헤어라인이 기본. 무거운 보더·강한 드롭섀도 금지.
- **섀도:** 소프트·저불투명 — `card 0 1px 2px rgba(13,13,13,.04)`, `lift 0 4px 20px rgba(13,13,13,.06)`. 웜 카드는 웜 섀도 `rgba(140,70,20,.06)`.
- **밴드 교대:** canvas ↔ paper 로 섹션 구분(보더 남발 대신).

---

## 4. 아이콘 (하네스 §2 하드룰)

- **아이콘 = `lucide-react`. 제품 시각 UI에 이모지 금지**(폰트·정렬·접근성·톤).
- 교체 매핑(기존 이모지 → lucide): 🎤→`mic` · 🔊→`volume-2` · ●녹음→`circle`(채움) · 정지→`square` · ✅→`check-circle-2` · 🙏→`heart-handshake` 또는 텍스트 · 🎙 진행자→`mic` · 검색→`search` · 새로만들기→`plus` · 더보기→`more-horizontal` · 프로젝트→`folder` · 벤치마크→`cpu` · 에러→`alert-triangle` · 재시도→`rotate-cw`.
- 이모지 교체는 **횡단 변경** → 코드 전 전수조사(스키마·컴포넌트·테스트, 하네스 §2).

---

## 5. 컴포넌트

### 앱 셸 (의뢰자) — 좌측 사이드바
- 브랜드 마크 + `search`(⌘K) + 네비(**실존 화면만**: 프로젝트·성능/벤치마크).
- **`프로젝트` 네비 아래 최근 프로젝트 서브리스트**(최대 8개, 최근 생성순 — 초과 시 `전체 보기` → `/projects`). 행 = 상태 점(작성 중=red·배포됨=obsidian·종료=grey) + 말줄임 제목, 활성 프로젝트는 네비와 동일하게 `blush` 배경/red 텍스트로 강조. 로딩 중엔 스켈레톤 2~3행, 0개면 옅은 안내문만("아직 프로젝트가 없어요") — **여기 CTA를 다시 넣지 않는다**(아래 원칙과 동일 이유). fetch 실패는 서브리스트만 조용히 숨긴다(사이드바 본체는 안 깨뜨림).
- **`+ 새 프로젝트` CTA 는 사이드바에 두지 않는다** — 각 페이지 헤더에만. 둘 다 두면 첫 화면에 같은 버튼이 두 개 보인다.
- **무인증:** 워크스페이스 전환·계정 메뉴·유저 아바타 **없음.** 사이드바 푸터 = "로그인 없이, 링크만 공유" 안내(무인증을 특징으로).
- 활성 네비 = `blush`/red-wash 배경 + red 아이콘.

### 콘텐츠 헤더바 (의뢰자) — `/projects/*` 공통
- 콘텐츠 컬럼 최상단의 슬림 바(52–56px, sticky). 화이트 바탕 + 하단 1px `silver` 보더. 사이드바 두 항목만으론 상세 화면에서 돌아갈 동선이 없던 문제(원래 있던 BackLink가 사이드바 셸 도입 때 빠짐)를 메운다.
- 뒤로 화살표(`arrow-left`, ≥44px 탭 타깃, `aria-label="뒤로"`) + 브레드크럼(obsidian/charcoal 텍스트, `grey` `/` 구분자).
- 경로별: `/projects`="프로젝트"(뒤로 없음) · `/projects/new`="← 프로젝트 / 새 프로젝트" · `/projects/benchmark`="성능 · 벤치마크"(뒤로 없음) · `/projects/[id]`="← 프로젝트 / {제목}"(제목은 fetch — 로딩 중 스켈레톤, 실패 시 원문 id 대신 "프로젝트"로 대체).
- 상세 화면 안의 탭(인터뷰 가이드/결과)은 그대로 둔다 — 이 바는 탭을 대체하지 않고 그 위 계층(어느 프로젝트인지·어떻게 돌아가는지)만 담당한다.

### 버튼
- **primary:** `brand-red` 배경/흰 글자, 8px. hover `red-dark`.
- **secondary:** 흰 배경/obsidian 글자, 1px `silver`.
- **ghost:** 투명/charcoal.
- (웜 표면에선 동일 구조, 웜 보더/배경.)

### 카드 (프로젝트)
- 흰 배경, 1px `silver`, 10px. 상태 배지(배포됨=obsidian칩·작성중=red-wash칩·종료=grey칩), `more-horizontal`, 하단 익명 카운트(응답 N·진행중 M) + 완료율.
- **응답자 아바타 스택 금지**(익명) → 카운트/진행률 시각화로.

### 상태 화면 (데모 티 제거 — "불러오는 중…" 텍스트 폐기)
- **로딩:** 스켈레톤 시머(텍스트 없음).
- **빈:** lucide 아이콘 + 안내문 + 액션(CTA).
- **에러:** `maroon` 톤 + `alert-triangle` + `rotate-cw` 재시도.

### 인터뷰 (응답자, 웜)
- **구조는 현행 코드 그대로**(상단 진행자+진행률 → 질문 히어로 → 텍스트영역+마이크/다음 · 자극물 2분할). 색만 웜.
- **진행자 = 작은 웜 오브**(peach→red 라디얼, 말할 때 살짝 맥동). 압도하지 않게 소형. 이모지 아님.
- 질문이 주인공(display 급). 마이크=웜 red, 다음=red. `volume-2` 다시 듣기.
- 녹음 중: red 채움 맥동 + mono 타이머 `● 0:04` + `square` 정지. 전사 중: 스켈레톤 한 줄.

### 벤치마크 (의뢰자) — 계측 스펙 §7 소비
- 헤드라인 **손익분기 S\*** + 곡선 · **M1**(SLA 세션슬롯/카드, "슬롯≠생성" caveat) · **M2**(세션당 Wh·idle 분해) · **M3**(κ 게이트) · 결과표(4구성) · 24h 전력 시계열.
- 수치는 **텔레메트리 mono**. **미측정=`—`(null)**, 추정치 금지(스펙 §8). NPU=red, GPU=grey.
- 데이터 소스·측정 방법 = `docs/specs/2026-07-23-rngd-benchmark-instrumentation.md`(별도 워크스트림).

### 랜딩 = 스플래시
- **최소 스플래시**(마케팅 랜딩 아님): 뷰포트 중앙에 로고 + 워드마크 + 한 줄 태그라인 + 단일 **`시작하기`** 버튼 → `/projects`(실제 서비스 진입).
- 서브 "가입 없이 바로 · 링크만 공유". 라이트 그라운드. 4스텝·프리뷰·푸터 등 마케팅 섹션 없음.

---

## 6. 반응형 · 접근성
- 모바일 우선(응답자). 터치 타깃 ≥44px. 키보드 포커스 링(brand-red, 2px).
- `prefers-reduced-motion` 존중(오브 맥동·시머 정지).
- 넓은 콘텐츠(표·차트)는 자체 `overflow-x:auto`, 본문 가로 스크롤 금지.

---

## 7. globals.css / tailwind 매핑 (구현 브리지 — Implement 단계)
- 이 문서의 토큰 → `globals.css` `@theme` CSS 변수 + `tailwind.config.ts` `colors`로 1:1.
- 기존 `--accent*`(sky-blue) 계열 → `brand-red` 계열로 리맵. 기존 error `#ef4444` → `maroon #6F2020`.
- 차트는 CSS 변수를 못 읽으니 리터럴 상수(데이터 팔레트 hex)를 컴포넌트에 둔다(현행 방식 유지).
- **이 매핑 편집은 코드 변경(Implement) — 하네스 게이트. design.md 확정 후 착수.**
